# 허브 씬 핵심 3대 상호작용 및 출격 페이드 연출 구현 계획

## Goal Description

사용자의 요청에 따라 허브 씬(`Hub_Scene`)의 씬 연출 및 기능을 아래 **3가지의 핵심 상호작용**으로 깔끔하게 정돈하고 집중합니다.

1. **차고 (Garage)**: 진입 후 F키를 눌러 스테이지 선택 패널(`garagePanel`)을 엽니다. 시작 버튼을 누르면 화면이 부드럽게 검게 물드는 **페이드아웃(Fade-Out) 연출**이 1초간 재생된 후 선택한 스테이지로 이동합니다.
2. **월드맵 (World Map)**: 진입 후 F키를 눌러 월드맵 패널(`worldMapPanel`)을 엽니다. 지도 화면에서 기본 지역과 위험 지역을 선택할 수 있는 UI 및 상세 브리핑을 제공합니다. (구 홀로그램 상호작용 연동)
3. **업그레이드 (Workshop)**: 진입 후 F키를 눌러 업그레이드 패널(`workshopPanel`)을 엽니다. 수거해 온 부품(칩)을 소모하여 기동성 및 체력 능력치를 영구 강화할 수 있습니다. (구 작업실 상호작용 연동)

이 세 가지 외의 다른 불필요한 기능(예: 기존 오토바이 주행 씬 `Ride_Scene` 연동 등)은 거치지 않고 바로 전투 씬으로 연결되도록 정리합니다.

---

## 구현 방식

### **차고 페이드아웃 연출의 구현 방식 (HubTransitionRunner)**:  
씬 전환 시 화면이 즉시 뚝 끊기며 로딩되는 것을 막기 위해, `DontDestroyOnLoad` 방식의 검은색 캔버스를 임시 생성하여 **1초 동안 부드럽게 화면을 페이드아웃(알파 0 ➔ 1) 시킨 후 다음 씬(Stage)을 로드**하고, 새 씬에 도달했을 때 새 씬의 트랜지션 매니저(`GameTransitionManager`)가 있으면 그곳으로 권한을 양도하고, 만약 없는 일반 씬이라면 스스로 다시 1초간 페이드인(알파 1 ➔ 0)으로 밝아지게 만드는 고급 연출 코드(`HubTransitionRunner` 컴포넌트)를 `HubUIManager.cs` 내부에 안전하게 탑재하고자 합니다.
이 방식은 씬 이동 중 코루틴이 씹히는 문제를 완벽히 차단합니다.

### **상호작용 포인트 연동 및 라벨 교정**:  
현재 `HubInteractionPoint.cs`에서 `Garage` 타입의 상호작용 포인트가 `hub.OpenWorldMap()`을 호출하도록 잘못 매핑되어 있습니다. 이를 수정하여 `Garage`는 `hub.OpenStageSelect()`, `Hologram`은 `hub.OpenWorldMap()`, `Workshop`은 `hub.OpenShop()`을 호출하도록 교정하고, 둥둥 떠다니는 F키 안내 팝업의 라벨 텍스트도 다음과 같이 직관적으로 변경할 예정입니다.
- 차고: `차고 진입 (스테이지 선택)`
- 홀로그램(월드맵): `월드맵 확인 (지역 선택)`
- 작업실: `기체 개조 (부품 강화)`
